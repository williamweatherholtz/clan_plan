services:

  app:
    image: ${APP_IMAGE:-clanplan:latest}
    build: .
    ports:
      - "${APP_PORT:-8080}:8080"
    env_file: .env
    environment:
      # Override DB host to use the compose service name
      DATABASE_URL: postgres://clanplan:${POSTGRES_PASSWORD:-clanplan}@db:5432/clanplan
      MEDIA_STORAGE_PATH: /data/media
    volumes:
      - media_data:/data/media
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: clanplan
      POSTGRES_USER: clanplan
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-clanplan}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U clanplan -d clanplan"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  # Dev-only mail catcher — remove or comment out for production.
  # Web UI at http://localhost:8025
  mailpit:
    image: axllent/mailpit:latest
    ports:
      - "8025:8025"   # web UI
      - "1025:1025"   # SMTP
    restart: unless-stopped

volumes:
  postgres_data:
  media_data:
